Today I Learned $ARGUMENTS.

Append it to the ./learnings.md in a format "- [<date>] <learning description>\n" where <date> is date in YYYY-MM-DD format, <learning description> is the description of what I've learned in a nice, concise yet capturing necessary information format. If I've learned multiple things (see examples), add multiple entries. You may also add any useful context (if applicable) if it is useful for future recall. The reason for this to be added to the file is because my knowledge when I learned this for the first time is very weak. You will later use the ./learnings.md file to check if I retained the knowledge and will come either in a form of a questionnaire or flashcards to build my "muscle memory".

Important. If in my learning I refer to CLAUDE's previous message such as if I say "I learned what you just said about Rust lifetimes", infer exact learning(s) that I have received by yourself based on the previous conversation. EVEN MORE IMPORTANT: If I point out to ONE thing, add exactly that thing, nothing else.

### Response

Respond with "<learning description> added to ./learnings.md"

### Example

In the previous message claude described multiple related concepts and gave descriptions of each.

When I say: Today I learned about all those concepts (I'm not calling out exact learning, I'm referring to the previous message by Claude)
You (claude) must add all concepts as separate entries.

When I say: Today I learned about one specific concept
You (claude) must add only that concept.

When I say: Today I learned that X and Y are related because ... (In this case I AM calling out the exact learning)
You (claude) must add the learning that I called out.
