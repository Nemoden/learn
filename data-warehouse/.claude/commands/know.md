Save this reference fact: $ARGUMENTS

Append to ./knowledge.md in format "- [<date>] <fact>\n" where <date> is YYYY-MM-DD.

This file is for **reference facts I'll look up later** — Parquet encoding flags, Iceberg metadata field names, AWS CLI command syntax, Athena SQL idioms, partition transform names, pyarrow API signatures, Firehose buffer limits, Kinesis shard quotas. Different from learnings.md which captures "aha moments."

If my wording is awkward or imprecise, rephrase for clarity and accuracy. If I'm vague (e.g., "save that limit"), infer the specific fact from the previous conversation.

### Response

Respond with "<fact> added to ./knowledge.md"
